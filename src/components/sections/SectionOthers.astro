---
import { translations } from '../../i18n/translations';

type SupportedLanguages = keyof typeof translations;
const { lang = 'es' } = Astro.props;
const t = translations[lang as SupportedLanguages];
---

<section class="section section-others" id="otros">
  <div class="content fade-in">
    <h2>{t['others.title']}</h2>
    <div class="info-card">
      <div class="info-message">
        <p class="message-text">
          {t['others.children']}
        </p>
      </div>
    </div>
    <div class="divider">
      <img src="/images/divider.svg" alt={t['others.divider.alt']} class="icon-image">
    </div>
    <div class="info-card">
      <div class="info-message">
        <p class="message-text">
          {t['others.allergies']}
        </p>
      </div>
      <div class="allergy-icons">
        <div class="allergy-icon">
          <img src="/images/allergens.webp" alt={t['others.allergens.alt']} class="icon-image">
        </div>
      </div>
    </div>
    <div class="divider">
      <img src="/images/divider.svg" alt={t['others.divider.alt']} class="icon-image">
    </div>
    <div class="info-card">
      <div class="info-message">
        <p class="message-text">
          {t['others.parking']}
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  h2 {
    font-size: 3rem;
    margin-bottom: 2rem;
    color: var(--secondary-color);
    text-align: center;
  }
  
  .section-others {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .info-card {
    max-width: 800px;
    text-align: center;
  }

  .info-message {
  }

  .message-text {
    font-size: 1.5rem;
    line-height: 1.6;
    color: #333;
    font-family: 'Italiana', serif;
  }

  .divider {
    width: 100%;
    height: 30px;
    margin: 2rem 0;
    display: flex;
    justify-content: center;
  }

  .divider svg {
    width: 80%;
    height: 100%;
  }

  .allergy-icons {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 3rem;
  }

  .icon-image {
    max-width: 100%;
    max-height: 100%;
  }

  .allergy-icon {
    width: 80px;
    height: 80px;
  }

  @media (max-width: 768px) {
    .section-others{
      height: 100vh;
      overflow: hidden;
    }
    .message-text {
      font-size: 1.2rem;
    }

    .allergy-icon {
      width: 100px;
      height: 100px;
    }
    
    .divider svg {
      width: 100%;
    }
  }
</style>